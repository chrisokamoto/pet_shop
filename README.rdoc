== Pet Shop

Para instalar bibliotecas:
- bower install (instala Bootstrap - precisa ter bower instalado no computador)
- bundle install

Para popular banco:
- rake db:migrate
- rake db:populate (popula banco com 1000 registros aleatórios)

Para rodar:
- rails s

Para rodar testes:
- rspec
